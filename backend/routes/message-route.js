// const express = require("express");
// const router = express.Router();
// const messageCtrl = require("../controllers/message-ctrl.js");
// const multer = require("../middleware/multer-config");
// const auth = require("../middleware/auth-config");

// router.post("/", auth, messageCtrl.createMeassage);
// router.get("/", auth, messageCtrl.findAllMessage);
// router.get("/:id", auth, multer, messageCtrl.findOneMessage);
// router.put("/:id", auth, multer, messageCtrl.modifyMessage);

// module.exports = router;
